# coding: utf-8

from django.apps import \
    AppConfig  # pragma: no cover


class BoilerplaceConfig(AppConfig):  # pragma: no cover
    """Define boilerplace django app config."""

    name = 'apps.boilerplace'
    app_label = 'boilerplace'
